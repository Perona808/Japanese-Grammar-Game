//this is the home page
//made the file pages so it can navigate between pages
function Home() {
  //makes the react component fo rthe home page
  return (
    //the return means return the html

    //everything in the brackets =JSX (which is the html that is inside of the typescript )

    <div className="app">
      <h1>Japanese Game</h1>
    </div>
  );
}

export default Home;
